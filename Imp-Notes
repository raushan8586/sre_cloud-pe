Factors to decide a region:
1. End users location
2. Latency
3. Service availability
4. Government Regulations
5. Cost


user --> ssh (port 22) --> linux server
user --> rdp (port 3389) --> windows server

user --> http (port 80) --> application running on port 80
user --> https (port 443) --> application running on port 443

ssh to linux server

ssh -i "may-sre-11am.pem" ubuntu@13.250.102.102

sudo su -
apt update -y
apt install apache2 -y
lsof -i -P -n | grep LISTEN

open port 80 in security group inbound rule
